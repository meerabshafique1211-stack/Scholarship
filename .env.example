# Copy to .env and fill in. Never commit .env.
DATABASE_URL="postgresql://user:password@localhost:5432/scholarship?schema=public"

# Phase 3 — authentication (e.g. Auth.js)
AUTH_SECRET=""

# Phase 4 — private document storage (S3-compatible)
STORAGE_BUCKET=""
STORAGE_REGION=""
STORAGE_ACCESS_KEY_ID=""
STORAGE_SECRET_ACCESS_KEY=""

# Phase 5 — LLM features (server-side only, never exposed to the browser)
ANTHROPIC_API_KEY=""
